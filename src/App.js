import './App.css';
// import './index.css'
import {Button} from 'antd';

const App = (props) => {
    return (
        <div>
            <button className="btn">Hello daisyUI</button>
            <Button type="primary">Button</Button>

        </div>
    );
};

export default App;
